/**
 * @description this class defines the marker attributes
 * @class Marker
 */
export interface Marker {
  /**
   * @property {string} lat
   */
  lat: string;
  /**
   * @property {string} lng
   */
  lng: string;
}
