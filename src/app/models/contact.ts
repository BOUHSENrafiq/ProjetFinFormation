/**
 * @description this class defines the contact attributes
 * @class Contact
 */
export default class Contact {
  name: string;
  /**
   * @property {string} email
   */
  email: string;
  /**
   * @property {number} phoneNumber
   */
  phoneNumber: number;
  /**
   * @property {string} subject - the topic of the message
   */
  subject: string;
  /**
   * @property {string} message
   */
  message: string;
}
